# Dependencias del proyecto (Python 3.10+)
# En Google Colab, torch ya viene preinstalado con soporte CUDA.
transformers>=4.40
huggingface_hub>=0.23
torch>=2.1
sentencepiece>=0.1.99   # requerido por el tokenizer de XLM-RoBERTa
accelerate>=0.30
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
